name: underwriting_payment_risk
version: '1.0.0'
config-version: 2
profile: underwriting_payment_risk
model-paths: [models]
test-paths: [tests]
clean-targets: [target, dbt_packages]
models:
  underwriting_payment_risk:
    staging:
      +materialized: view
    marts:
      +materialized: table
